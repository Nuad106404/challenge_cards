# Use http://10.0.2.2:3001/api when running on Android emulator
API_BASE_URL=http://10.0.2.2:3001/api

# AdMob ad unit IDs — used as local dev fallback only.
# Production IDs are managed from the admin panel (Global Config page)
# and delivered to the app at runtime via the API.
ADMOB_BANNER_ID=ca-app-pub-xxxxxxxxxxxxxxxx/xxxxxxxxxx
ADMOB_INTERSTITIAL_ID=ca-app-pub-xxxxxxxxxxxxxxxx/xxxxxxxxxx

# NOTE: ADMOB_APP_ID is a BUILD-TIME value and must be set in
# android/local.properties (not here). See android/local.properties.example.
